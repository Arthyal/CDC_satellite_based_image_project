# CDC_satellite_based_image_project
All the code files should be in the same directory along with train(1).xlsx and the test2.xlsx
First run the fetcher.py file , then run the preprocessing.ipynb and finally run model_training.ipynb
In the fetcher.py file check the code lines from 16-24 and ensure that the correct directory path is there , also give the corret mapbox token in the line 10.
Rest the code will do y itself , in the end there would a csv file of predicted price along with columns. 
